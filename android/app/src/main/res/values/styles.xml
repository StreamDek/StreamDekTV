<resources>
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:windowBackground">@android:color/black</item>
  </style>
  <!-- CloudStreamSettingsActivity: an invisible window over StreamDek, so a plugin's own settings
       dialog or bottom sheet reads as a popup. MaterialComponents because CloudStream extensions
       build those with Material classes and styles, as CloudStream's own theme allows. -->
  <style name="Theme.StreamDek.PluginSettings" parent="Theme.MaterialComponents.NoActionBar">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:backgroundDimEnabled">false</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/boot_splash</item>
  </style>
</resources>
